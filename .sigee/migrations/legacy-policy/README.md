# Legacy Policy Archive

This folder stores archived policy documents from pre-planner runtime models.

- These files are kept only for migration traceability.
- They are non-normative and must not be used as runtime policy inputs.
- Current normative runtime policy is in `.sigee/policies/`.
