# DAG Pipelines

Store tracked baseline pipeline definitions.

- Runtime-generated pipeline files are emitted under `${SIGEE_RUNTIME_ROOT:-.sigee/.runtime}/dag/pipelines/`.
- Planner/developer should treat runtime pipelines as volatile execution artifacts.
